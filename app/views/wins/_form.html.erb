<div class="row row-spacer">
  <h1>
    <%= form_for @win do |f| %>
    <div class="col-md-10">
      <%= f.text_field :body, placeholder: "Add and complete 3 wins each day", class: "form-control input-lg input-super", :autofocus => true %>
    </div>
    <div class="col-md-2">
      <%= f.submit "Add", class: 'btn btn-lg btn-super btn-primary full-width pull-right' %>
    </div>
    <% end %>
  <h1>
</div>
