<%= render 'layouts/messages' %>

<% if current_win_count < 3 && !params[:history].present? %>
  <%= render 'form' %>
<% end %>

<% if params[:history].present? %>
  <%= render partial: "archived_win", collection: @wins %>
<% else %>
  <%= render @wins %>
<% end %>
