<!DOCTYPE html>
<html lang="en">
  <head>
    <link href='http://fonts.googleapis.com/css?family=Playfair+Display:700italic' rel='stylesheet' type='text/css'>    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Wins</title>
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
    <%= csrf_meta_tags %>
  </head>
  <body>
    <%= render 'layouts/navigation' %>
    <hr>
    <div class="container">
      <%= yield %>
    </div>
    <% if logged_in? %>
      <div class="footer text-center lead">
        Time is now <%=  format_time(Time.current) %>. <%= Time.current.seconds_until_end_of_day %> seconds until the end of the day.
      </div>
    <% end %>
  </body>
</html>
